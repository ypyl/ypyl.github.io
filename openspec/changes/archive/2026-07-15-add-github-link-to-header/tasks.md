## 1. Implementation

- [x] 1.1 Add "GitHub" link to `_includes/header.html` inside the `.trigger` div, after the `{% endfor %}` loop, using `class="page-link"` and href `https://github.com/{{ site.github_username }}`
