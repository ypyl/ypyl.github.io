## 1. CSS Fix

- [x] 1.1 Add `width: 100%` to `.news-carousel.ready .news-carousel-dots` inside the `@media screen and (max-width: 600px)` block in `assets/css/style.css`

## 2. Verification

- [x] 2.1 Verify dots are horizontally centered on mobile viewports (≤600px) with 2+ views
- [x] 2.2 Verify desktop layout (vertical dots on the right) is unchanged
